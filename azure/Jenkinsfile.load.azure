pipeline{
    agent any

    stages {
        stage("Checkout") {
            steps {
                checkout scm
            }
        }

        stage("Terraform init"){
            steps {
                script {
                    sh 'terraform init'
                }
            }
        }  
    }
}
