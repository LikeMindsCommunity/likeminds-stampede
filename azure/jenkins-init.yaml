#cloud-config
package_update: true
packages:
  - docker.io

runcmd:
  - systemctl start docker
  - systemctl enable docker
  - docker run -d --name jenkins -p 80:8080 -p 50000:50000 -v jenkins_data:/var/jenkins_home jenkins/jenkins:lts
